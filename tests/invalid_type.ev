struct A { int i; int j1; bool b; struct A a; };
struct B { struct A a; };

int i, j, k;
struct B b, bb, bbb;
bool bob;

fun main() int {
   struct A a;
   int i, j, k;
   bool b;
   int h;

   b = false;
   i = 5;

   # Can't do this
   h = i + b;

   # Can't do this either (return type)
   return false;
}
