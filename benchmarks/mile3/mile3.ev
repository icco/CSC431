# REMEMBER TO MODIFY the .c files and the INPUT and OUTPUT files.

int global1;
int global2;

# adds two numbers
fun sum (int a, int b) int
{
   int c, d, e, f;
   c = 10;
   d = 12;

   return (a + b);
}

# main function
fun main () int 
{
   global1 = 10000;
   global2 = 2;

   if ( 1 < 2 ) {
      print (sum (global1, global2)) endl;
   } else {
      print (sum (2, 2)) endl;
   }

   return 0;
}
