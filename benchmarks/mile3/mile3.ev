# REMEMBER TO MODIFY the .c files and the INPUT and OUTPUT files.

# adds two numbers
fun sum (int a, int b) int
{
    return (a + b);
}

# main function
fun main () int 
{
   if ( 1 < 2 ) {
      print (sum (10000, 2)) endl;
   } else {
      print (sum (2, 2)) endl;
   }

   return 0;
}
